{
  "name": "task-buddy",
  "private":true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "client:dev:start":"yarn workspace client dev",
    "build:client": "yarn workspace client build",
    "server:dev:start":"yarn workspace server start:dev",
    "server:test":"yarn workspace server test"
  }
}
