applications:
  - frontend:
      phases:
        preBuild:
          commands:
            - cd client
            - yarn install
        build:
          commands:
            - yarn build
      artifacts:
        baseDirectory: client/dist # use `client/build` for CRA
        files:
          - "**/*"
      cache:
        paths:
          - client/node_modules/**
